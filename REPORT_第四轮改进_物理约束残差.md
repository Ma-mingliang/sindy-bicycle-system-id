# 第四轮改进：物理约束残差测试报告

> 测试时间：2026-06-22
> 改进方向：给NN残差添加物理约束

---

## 一、改进思路

### 1.1 原始方法

NN残差无约束，可以输出任意值。

### 1.2 改进方法

给NN残差添加物理约束：
1. **有界约束**：使用tanh限制输出范围（±max_residual）
2. **能量约束**：限制残差范数，避免无限增加系统能量
3. **平滑约束**：减少残差突变，使输出更平滑

**核心代码**：
```python
class PhysicsConstrainedResidualNet(nn.Module):
    def forward(self, s, a=None):
        raw_residual = self.net(x)
        # 有界约束：tanh限制范围
        bounded_residual = torch.tanh(raw_residual) * self.max_residual
        return bounded_residual

# 训练损失
total_loss = mse_loss + energy_weight * energy_loss + 0.001 * smooth_loss
```

---

## 二、测试结果

### 2.1 NeuralODE + 物理约束残差

| 步数 | 纯基线 (rad) | 物理约束 (rad) | 改进倍率 |
|------|-------------|---------------|----------|
| 1 | 0.00004 | 0.00009 | 2.10x (恶化) |
| 5 | 0.02772 | 0.02386 | 0.86x (改善) |
| 10 | 0.08985 | 0.08276 | 0.92x (改善) |
| 20 | 0.17155 | 0.16025 | 0.93x (改善) |
| 50 | 0.18063 | 0.17437 | 0.97x (改善) |
| 100 | 0.26460 | 0.23680 | 0.89x (改善) |
| 200 | 0.19304 | 0.21886 | 1.13x (恶化) |
| 500 | 0.16470 | **0.28437** | 1.73x (恶化) |

---

## 三、与其他方法对比

| 方法 | 500步MAE (rad) | 对比 |
|------|----------------|------|
| NeuralODE + OOD + DAgger + 0.3 | 0.48 | 基准 |
| NeuralODE + 多步损失 | **0.097** | 第三轮改进（最佳） |
| NeuralODE + 物理约束残差 | 0.284 | 第四轮改进（恶化） |

---

## 四、问题分析

### 4.1 为什么物理约束残差效果不好？

**原因**：
1. **tanh限制太严格**：残差被限制在±0.1范围内，限制了NN的学习能力
2. **能量约束可能过于保守**：限制残差范数可能导致残差太小
3. **平滑约束可能不必要**：残差本身应该是平滑的，额外约束可能干扰学习

### 4.2 具体问题

- 500步时恶化72%（0.165→0.284）
- 中间步数有轻微改善，但长步数恶化
- 说明物理约束在长步数时反而有害

---

## 五、结论与建议

### 5.1 结论

1. **物理约束残差效果不好**（500步恶化72%）
2. **tanh限制太严格**，限制了NN的学习能力
3. **多步损失训练**（第三轮改进）效果更好

### 5.2 后续改进方向

1. **放弃物理约束残差**
2. **继续优化多步损失训练**
3. **尝试贝叶斯神经网络**

---

## 六、相关代码

| 文件 | 说明 |
|------|------|
| `test_physics_constrained.py` | 物理约束残差测试代码 |

---

## 七、运行命令

```bash
# 运行物理约束残差测试
"E:/Anaconda/envs/DL/python.exe" "D:/系统辨识作业/sindy_bicycle/test_physics_constrained.py"
```

---

*报告生成时间：2026-06-22*
