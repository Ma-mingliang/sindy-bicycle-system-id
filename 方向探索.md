# 方向探索：世界模型获取方式

> 记录时间：2026-06-20

---

## 一、世界模型概念

在Model-Based RL中，世界模型是环境的"模拟器"——给定当前状态s和动作a，预测下一状态s'。用于生成虚拟经验（virtual rollout），减少真实环境交互。

## 二、世界模型获取方式

### 方式1：纯学习（最常见）

- 从真实环境交互数据中学习
- 用神经网络拟合 `(s, a) → s'`
- MBPO原文用的是ensemble of neural networks（多个NN取均值/方差来估计不确定性）
- 需要大量真实数据（几万到几十万步）

### 方式2：解析模型（需要已知物理参数）

- 从物理方程推导（如Meijaard 2007）
- 不需要训练数据
- 精度取决于模型假设是否正确

### 方式3：混合（物理先验 + 学习残差）

- 用解析模型做基线，NN学残差
- 比纯学习需要更少数据
- 比纯解析能补偿建模误差

## 三、我们的自行车情况

### 当前项目

使用Meijaard 2007基准自行车模型（已知所有参数），直接用解析模型作为世界模型。

### 后期实物

用户的自行车是**双轮可转向（dual-steer）**，不是常规自行车。Meijaard模型不适用。

### 实物数据获取模型的方法

**推荐：SINDy系统辨识**

原因：
- SINDy**不需要知道模型结构**，从数据中自动发现
- 双轮可转向的自由度更多（可能4-6维状态），手工推导方程很复杂
- SINDy输出可解释的稀疏方程，帮助理解系统动力学
- 如果精度不够，可以扩展库（加入三角函数等）

**具体步骤：**

1. 在实物上采集开环数据（施加随机力矩，记录状态轨迹）
2. 用SINDy辨识：`(s, a) → s'` 的映射
3. 如果精度不够，扩展SINDy库（加入sin/cos等）
4. 用辨识出的模型作为MBPO的世界模型

**备选方案：**

- NN动力学模型（端到端学习，不需要任何先验知识）
- SINDy+NN混合（SINDy做基线，NN学残差）

---

## 四、Model-Based vs Model-Free 对比

| 对比项 | Model-Free (TD3/SAC) | Model-Based (MBPO) |
|--------|---------------------|-------------------|
| 是否需要世界模型 | 否 | 是 |
| 样本效率 | 低（几十万~几百万步） | 高（几万步） |
| 训练稳定性 | 较好 | 受世界模型精度影响 |
| 实现复杂度 | 简单 | 较复杂 |
| 适用场景 | 仿真环境（数据便宜） | 真实环境（数据昂贵） |

---

## 五、从真实数据拟合自行车模型的方法

### 方法1：SINDy（稀疏动力学辨识）

- **原理**：在候选函数库上做稀疏回归，自动发现动力学方程
- **优点**：可解释、稀疏、不需要假设模型结构
- **缺点**：依赖库的选择，复杂非线性可能需要扩展库
- **适用**：结构未知、希望得到解析方程

### 方法2：神经网络动力学模型（端到端学习）

- **原理**：用NN直接学习 `(s, a) → s'` 的映射
- **优点**：不需要任何先验知识，拟合能力强
- **缺点**：黑盒、需要大量数据、泛化能力有限
- **适用**：数据充足、不关心可解释性
- **典型方法**：MBPO原文用ensemble of NNs

### 方法3：Neural ODE

- **原理**：用NN学习连续时间动力学 `ds/dt = f(s, a)`，用ODE积分器求解
- **优点**：连续时间建模、比离散NN更物理一致
- **缺点**：训练慢（需要ODE求解器）、实现复杂
- **适用**：需要连续时间模型、数据量适中

### 方法4：Gaussian Process（高斯过程）

- **原理**：非参数贝叶斯方法，学习 `(s, a) → s'` 的分布
- **优点**：提供不确定性估计、小数据效果好
- **缺点**：计算复杂度O(n³)、大数据不可行
- **适用**：数据量小（几百~几千）、需要不确定性

### 方法5：物理启发神经网络（PINN）

- **原理**：在NN损失函数中加入物理约束（如能量守恒、对称性）
- **优点**：结合数据和物理先验、比纯NN更泛化
- **缺点**：需要知道部分物理约束、实现复杂
- **适用**：知道部分物理规律、数据量适中

### 方法6：参数辨识（假设已知模型结构）

- **原理**：假设动力学方程形式已知（如Meijaard方程），用数据拟合参数
- **优点**：参数有物理意义、数据需求少
- **缺点**：需要知道模型结构
- **适用**：已知方程形式、只需拟合参数

### 方法7：符号回归（Symbolic Regression）

- **原理**：搜索数学表达式空间，找到拟合数据的最佳方程
- **优点**：自动发现方程形式、可解释
- **缺点**：搜索空间大、计算昂贵
- **适用**：希望得到解析方程、SINDy不够灵活时

### 方法8：SINDy + NN残差（混合方法）

- **原理**：SINDy做基线（可解释部分），NN学残差（复杂非线性部分）
- **优点**：兼顾可解释性和拟合能力
- **缺点**：需要训练两个模型
- **适用**：SINDy精度不够、但希望保留可解释性

### 方法对比总结

| 方法 | 数据需求 | 可解释性 | 精度 | 实现难度 | 适用场景 |
|------|---------|---------|------|---------|---------|
| SINDy | 中 | 高 | 中 | 低 | 结构发现 |
| NN动力学 | 高 | 低 | 高 | 低 | 数据充足 |
| Neural ODE | 中 | 低 | 高 | 高 | 连续时间 |
| 高斯过程 | 低 | 中 | 中 | 中 | 小数据+不确定性 |
| PINN | 中 | 中 | 高 | 高 | 已知部分物理 |
| 参数辨识 | 低 | 高 | 高 | 低 | 已知模型结构 |
| 符号回归 | 中 | 高 | 中 | 中 | 方程发现 |
| SINDy+NN | 中 | 中 | 高 | 中 | 精度+可解释性 |

### 针对双轮可转向自行车的建议

**首选：SINDy系统辨识**

1. 数据需求适中（几千~几万步）
2. 自动发现动力学结构
3. 输出可解释的方程
4. 如果精度不够，可扩展库或加NN残差

**如果SINDy不够：**

- 数据充足 → NN动力学模型
- 数据少 + 需要不确定性 → 高斯过程
- 知道部分物理约束 → PINN
