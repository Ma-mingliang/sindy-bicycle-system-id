# 第三轮改进：多步损失训练测试报告

> 测试时间：2026-06-22
> 改进方向：用多步rollout loss训练NN残差

---

## 一、改进思路

### 1.1 原始方法

只训练单步残差：`loss = MSE(pred_residual, true_residual)`

### 1.2 改进方法

在单步损失基础上增加多步rollout损失：
- 用当前NN进行多步rollout
- 计算rollout的累积误差
- 总损失 = 单步损失 + 多步损失 × 权重

**核心代码**：
```python
# 单步损失
single_loss = MSE(pred, yb)

# 多步损失
for _ in range(2):  # 2个起点
    for step in range(multistep_length):
        # 混合预测
        s_next = baseline.predict(s, tau) + nn_model(s, a) * delta_std * scale
        # 真实下一步
        s_real = real_step(s, tau)
        # 累积误差
        rollout_loss += MSE(s_next, s_real)

# 总损失
total_loss = single_loss + 0.5 * rollout_loss
```

---

## 二、测试结果

### 2.1 NeuralODE + 多步损失(10步)

| 步数 | 纯基线 (rad) | 多步损失 (rad) | 改进倍率 |
|------|-------------|---------------|----------|
| 1 | 0.00022 | 0.00094 | 4.18x (恶化) |
| 5 | 0.00175 | 0.13016 | 74.44x (恶化) |
| 10 | 0.01156 | 0.08223 | 7.11x (恶化) |
| 20 | 0.06466 | 0.17619 | 2.72x (恶化) |
| 50 | 0.27338 | 0.25744 | 0.94x (改善) |
| 100 | 1.70728 | 0.11307 | 0.07x (大幅改善) |
| 200 | 4.06126 | 0.11156 | 0.03x (大幅改善) |
| 500 | 18.39215 | **0.09727** | 0.01x (大幅改善) |

---

## 三、与最佳方案对比

| 方法 | 500步MAE (rad) | 对比 |
|------|----------------|------|
| GP + OOD + DAgger + 0.3 | 0.15 | 基准（GP基线） |
| GP + 自适应残差缩放 | 0.081 | 第一轮改进 |
| NeuralODE + OOD + DAgger + 0.3 | 0.48 | 基准（NeuralODE基线） |
| **NeuralODE + 多步损失** | **0.097** | **第三轮改进（改善80%！）** |

---

## 四、分析

### 4.1 为什么多步损失有效？

**原因**：
1. 多步损失让NN学习到长期影响，不只优化单步预测
2. 在rollout中，NN的错误会累积，这迫使NN学习更稳健的残差
3. 减少了分布偏移问题（因为训练时就包含了rollout数据）

### 4.2 短步数为什么恶化？

**原因**：
1. 多步损失优先优化长步数表现，牺牲了短步数精度
2. 这是一个权衡：长步数改善80%，但短步数恶化

### 4.3 与自适应残差缩放的关系

- **自适应残差缩放**：改善GP基线（0.15→0.081，46%改善）
- **多步损失训练**：改善NeuralODE基线（0.48→0.097，80%改善）
- 两者可以结合使用

---

## 五、结论与建议

### 5.1 结论

1. **多步损失训练对NeuralODE基线非常有效**（80%改善）
2. **NeuralODE + 多步损失 = 0.097 rad**，比GP + 自适应残差缩放(0.081 rad)接近
3. **短步数恶化**是权衡结果，长步数大幅改善

### 5.2 后续改进方向

1. **结合多步损失 + 自适应残差缩放**
2. **对GP基线也尝试多步损失**（但需要解决速度问题）
3. **尝试物理约束残差**

---

## 六、相关代码

| 文件 | 说明 |
|------|------|
| `test_multistep_loss.py` | 多步损失训练测试代码 |

---

## 七、运行命令

```bash
# 运行多步损失训练测试（NeuralODE基线）
"E:/Anaconda/envs/DL/python.exe" "D:/系统辨识作业/sindy_bicycle/test_multistep_loss.py"
```

---

*报告生成时间：2026-06-22*
