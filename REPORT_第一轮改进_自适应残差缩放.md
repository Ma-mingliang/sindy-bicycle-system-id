# 第一轮改进：自适应残差缩放测试报告

> 测试时间：2026-06-21
> 改进方向：根据OOD距离动态调整残差缩放系数

---

## 一、改进思路

### 1.1 原始方法

残差缩放固定为0.3，无论输入是否接近训练分布。

### 1.2 改进方法

根据OOD距离动态调整残差缩放系数：
- 距离<1σ时scale接近max_scale（0.5）
- 距离>3σ时scale接近0
- 用sigmoid函数实现平滑过渡

**核心代码**：
```python
def compute_scale(self, x: np.ndarray) -> float:
    z = np.abs(x - self.mean) / self.std
    mahal_dist = np.max(z)

    k = 3.0  # 控制sigmoid陡峭程度
    normalized_dist = (mahal_dist - 1.0) / (self.threshold - 1.0)
    clipped_input = max(-10.0, min(10.0, k * normalized_dist * 4))
    sigmoid = 1.0 / (1.0 + math.exp(clipped_input))
    scale = sigmoid * self.max_scale

    return scale
```

---

## 二、测试结果

### 2.1 GP + 自适应残差缩放

| 步数 | 纯基线 (rad) | 自适应混合 (rad) | 改进倍率 |
|------|-------------|-----------------|----------|
| 1 | 0.00000 | 0.00132 | 2031.66x (恶化) |
| 5 | 0.00000 | 0.11433 | 29779.43x (恶化) |
| 10 | 0.00001 | 0.10127 | 7542.35x (恶化) |
| 20 | 0.00008 | 0.22291 | 2896.43x (恶化) |
| 50 | 0.00066 | 0.06646 | 100.55x (恶化) |
| 100 | 0.00672 | 0.05946 | 8.85x (恶化) |
| 200 | 0.64987 | 0.08935 | 0.14x (改善) |
| 500 | 7.51669 | **0.08130** | 0.01x (大幅改善) |

**自适应scale统计**：
- 平均：0.4741
- 中位：0.4778
- 最小：0.0693
- 最大：0.4983
- >0.1的比例：99.96%

### 2.2 NeuralODE + 自适应残差缩放

| 步数 | 纯基线 (rad) | 自适应混合 (rad) | 改进倍率 |
|------|-------------|-----------------|----------|
| 1 | 0.00016 | 0.00109 | 6.90x (恶化) |
| 5 | 0.00362 | 0.08122 | 22.43x (恶化) |
| 10 | 0.02772 | 0.16088 | 5.80x (恶化) |
| 20 | 0.12617 | 0.36209 | 2.87x (恶化) |
| 50 | 0.33711 | 0.38152 | 1.13x (恶化) |
| 100 | 1.04711 | 0.20748 | 0.20x (改善) |
| 200 | 2.55456 | 0.28879 | 0.11x (改善) |
| 500 | 8.70618 | **0.30516** | 0.04x (大幅改善) |

**自适应scale统计**：
- 平均：0.4958
- 中位：0.4970
- 最小：0.3149
- 最大：0.4971
- >0.1的比例：100.00%

---

## 三、对比分析

### 3.1 与固定scale=0.3对比

| 方法 | 固定0.3 (rad) | 自适应 (rad) | 改善幅度 |
|------|--------------|--------------|----------|
| GP | 0.15 | **0.0813** | 46% |
| NeuralODE | 0.48 | **0.305** | 36% |

### 3.2 关键发现

1. **长步数时效果很好**：500步MAE改善36-46%
2. **短步数时恶化**：因为scale太大（平均0.47 vs 固定0.3）
3. **自适应scale的平均值比固定0.3大**：说明在分布内时scale更大，充分利用残差

---

## 四、问题分析

### 4.1 短步数恶化原因

**问题**：自适应scale在分布内时太大（平均0.47），导致残差影响过大。

**原因**：
- 当前实现：距离<1σ时scale接近max_scale（0.5）
- 但实际上，即使在分布内，残差也有噪声
- 过大的scale会放大噪声，导致短步数时误差增大

**解决方案**：
- 降低max_scale到0.3（和之前一样）
- 或者调整sigmoid函数，使scale在分布内更小

### 4.2 长步数改善原因

**原因**：
- 长步数时，分布偏移不可避免
- 自适应scale在分布外时更小（接近0），减少了错误残差的影响
- 这比固定0.3更保守，避免了正反馈放大误差

---

## 五、结论与建议

### 5.1 结论

1. **自适应残差缩放在长步数时有效**（改善36-46%）
2. **但短步数时恶化**（因为scale太大）
3. **需要调整max_scale参数**，平衡长步数和短步数的表现

### 5.2 后续改进方向

1. **降低max_scale到0.3**：保持和之前一样的最大缩放系数
2. **调整sigmoid函数**：使scale在分布内更小
3. **分步数自适应**：短步数时用小scale，长步数时用大scale

---

## 六、相关代码

| 文件 | 说明 |
|------|------|
| `test_adaptive_scale.py` | 自适应残差缩放测试代码 |
| `improved_hybrid.py` | 原始改进版混合方法（固定scale=0.3） |

---

## 七、运行命令

```bash
# 运行自适应残差缩放测试
"E:/Anaconda/envs/DL/python.exe" "D:/系统辨识作业/sindy_bicycle/test_adaptive_scale.py"
```

---

*报告生成时间：2026-06-21*
