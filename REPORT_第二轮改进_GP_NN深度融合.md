# 第二轮改进：GP + NN更深度融合测试报告

> 测试时间：2026-06-22
> 改进方向：用GP的不确定性指导NN残差的置信度

---

## 一、改进思路

### 1.1 原始方法

GP和NN独立训练，NN只是学习残差，不受GP不确定性影响。

### 1.2 改进方法

用GP的不确定性来调整NN残差的权重：
- GP预测时返回均值和方差
- 方差大时说明GP不确定，此时NN残差的置信度应该降低
- 用GP方差来调整NN残差的权重：方差大→权重小，方差小→权重大

**核心代码**：
```python
def uncertainty_aware_predict(baseline, nn_model, ood_detector, s, tau, ...):
    s_next_base, variance = baseline.predict_with_uncertainty(s, tau)

    # 计算归一化方差
    normalized_variance = np.mean(variance) / (np.mean(delta_std ** 2))

    # 用方差调整scale：方差大→scale小，方差小→scale大
    k = 5.0
    clipped_input = max(-10.0, min(10.0, k * (normalized_variance - 0.5)))
    variance_scale = 1.0 / (1.0 + math.exp(clipped_input))
    adaptive_scale = base_scale * variance_scale

    return s_next_base + delta_nn * adaptive_scale, adaptive_scale, normalized_variance
```

---

## 二、测试结果

### 2.1 GP + 不确定性感知残差

| 步数 | 纯基线 (rad) | 不确定性感知 (rad) | 改进倍率 |
|------|-------------|-------------------|----------|
| 1 | 0.00002 | 0.00227 | 124.07x (恶化) |
| 5 | 0.00054 | 0.20292 | 378.30x (恶化) |
| 10 | 0.00254 | 0.11106 | 43.67x (恶化) |
| 20 | 0.01205 | 0.21682 | 18.00x (恶化) |
| 50 | 0.03584 | 0.15307 | 4.27x (恶化) |
| 100 | 0.39938 | 0.25707 | 0.64x (改善) |
| 200 | 72.39454 | 0.22481 | 0.00x (大幅改善) |
| 500 | 329.95186 | **0.24639** | 0.00x (大幅改善) |

**自适应scale统计**：
- 平均：0.2772
- 中位：0.2772
- 最小：0.2772
- 最大：0.2772
- >0.1的比例：100.00%

**GP不确定性统计**：
- 平均归一化方差：0.0000
- 中位归一化方差：0.0000

---

## 三、问题分析

### 3.1 核心问题

**GP的方差几乎为0**，说明GP对自己的预测非常自信（即使预测是错的）。

### 3.2 原因分析

1. **GP的方差不能反映真实的不确定性**
   - GP的方差是基于核函数计算的
   - 当训练数据密集时，GP会认为预测非常可靠
   - 但实际上，GP的外推能力很差，方差不能反映外推的不确定性

2. **自适应scale没有生效**
   - 由于GP方差几乎为0，normalized_variance≈0
   - sigmoid函数输出≈0.5，scale≈0.2772（固定值）
   - 没有实现"方差大→scale小"的效果

3. **短步数恶化原因**
   - scale=0.2772比固定0.3小，但仍然导致短步数恶化
   - 说明即使小的残差也会在短步数时放大误差

---

## 四、与第一轮改进对比

| 方法 | 500步MAE (rad) | 对比固定0.3 |
|------|----------------|-------------|
| 固定0.3 | 0.15 | 基准 |
| 自适应残差缩放 | **0.0813** | 改善46% |
| 不确定性感知残差 | 0.246 | 恶化64% |

**结论**：
- 自适应残差缩放（第一轮改进）效果很好
- 不确定性感知残差（第二轮改进）效果不好
- 原因：GP的方差不能反映真实的不确定性

---

## 五、结论与建议

### 5.1 结论

1. **GP + NN更深度融合（用GP不确定性指导NN残差）效果不好**
2. **原因**：GP的方差不能反映真实的不确定性
3. **自适应残差缩放（第一轮改进）效果更好**

### 5.2 后续改进方向

1. **放弃GP不确定性指导**：GP的方差不能反映真实的不确定性
2. **继续优化自适应残差缩放**：调整max_scale参数
3. **尝试其他改进方向**：
   - 多步损失训练
   - 物理约束残差
   - 贝叶斯神经网络

---

## 六、相关代码

| 文件 | 说明 |
|------|------|
| `test_gp_nn_fusion.py` | GP+NN深度融合测试代码 |
| `test_adaptive_scale.py` | 自适应残差缩放测试代码（第一轮改进） |

---

## 七、运行命令

```bash
# 运行GP+NN深度融合测试
"E:/Anaconda/envs/DL/python.exe" "D:/系统辨识作业/sindy_bicycle/test_gp_nn_fusion.py"
```

---

*报告生成时间：2026-06-22*
