# 5轮改进总结报告

> 测试时间：2026-06-22
> 基线方案：GP + OOD + DAgger + 0.3 = 0.15 rad

---

## 一、5轮改进结果总览

| 轮次 | 改进方向 | 基线方法 | 500步MAE (rad) | 对比基线 | 结论 |
|------|----------|----------|----------------|----------|------|
| 0 | 基准 | GP + OOD + DAgger + 0.3 | 0.15 | - | 基准 |
| 1 | 自适应残差缩放 | GP | **0.081** | 改善46% | 有效 |
| 2 | GP+NN深度融合 | GP | 0.246 | 恶化64% | 失败 |
| 3 | 多步损失训练 | NeuralODE | **0.097** | 改善80% | 有效 |
| 4 | 物理约束残差 | NeuralODE | 0.284 | 恶化72% | 失败 |
| 5 | MC Dropout | NeuralODE | 1.92 | 改善14% | 一般 |

---

## 二、最佳方案排名

| 排名 | 方法 | 500步MAE (rad) | 改进轮次 |
|------|------|----------------|----------|
| 1 | **NeuralODE + 多步损失** | **0.097** | 第三轮 |
| 2 | **GP + 自适应残差缩放** | **0.081** | 第一轮 |
| 3 | GP + OOD + DAgger + 0.3 | 0.15 | 基准 |
| 4 | NeuralODE + 物理约束残差 | 0.284 | 第四轮 |
| 5 | GP + NN深度融合 | 0.246 | 第二轮 |
| 6 | NeuralODE + MC Dropout | 1.92 | 第五轮 |

---

## 三、有效改进分析

### 3.1 第一轮：自适应残差缩放（GP基线，改善46%）

**原理**：根据OOD距离动态调整残差缩放系数
- 距离<1σ时scale接近0.5
- 距离>3σ时scale接近0
- 用sigmoid函数实现平滑过渡

**为什么有效**：
- 长步数时分布偏移不可避免
- 自适应scale在分布外时更小（接近0），减少了错误残差的影响
- 比固定0.3更保守，避免了正反馈放大误差

### 3.2 第三轮：多步损失训练（NeuralODE基线，改善80%）

**原理**：在单步损失基础上增加多步rollout损失
- 用当前NN进行多步rollout
- 计算rollout的累积误差
- 总损失 = 单步损失 + 多步损失 × 权重

**为什么有效**：
- 多步损失让NN学习到长期影响，不只优化单步预测
- 在rollout中，NN的错误会累积，这迫使NN学习更稳健的残差
- 减少了分布偏移问题（因为训练时就包含了rollout数据）

---

## 四、失败改进分析

### 4.1 第二轮：GP+NN深度融合（失败）

**原因**：GP的方差不能反映真实的不确定性
- GP的方差几乎为0（平均归一化方差=0.0000）
- GP对自己的预测非常自信（即使预测是错的）
- 自适应scale固定在0.2772，没有根据不确定性动态调整

### 4.2 第四轮：物理约束残差（失败）

**原因**：tanh限制太严格
- 残差被限制在±0.1范围内，限制了NN的学习能力
- 能量约束可能过于保守
- 500步时恶化72%

### 4.3 第五轮：MC Dropout（一般）

**原因**：不确定性估计太嘈杂
- 平均3.72，最大30.03，方差很大
- 不确定性高时残差权重被过度减少
- 10次采样可能不够

---

## 五、核心发现

### 5.1 有效改进的共同特点

1. **保守策略**：在分布外时减少残差权重
2. **长期优化**：不只优化单步预测，考虑长期影响
3. **动态调整**：根据输入分布动态调整残差权重

### 5.2 失败改进的共同特点

1. **过度约束**：限制了NN的学习能力
2. **不准确的不确定性估计**：GP方差、MC Dropout方差都不够准确
3. **过于激进的自适应**：不确定性高时残差权重被过度减少

---

## 六、后续方向

### 6.1 可尝试的改进

1. **结合自适应残差缩放 + 多步损失训练**
2. **增加MC Dropout采样次数**（从10次增加到50次或100次）
3. **尝试Deep Ensemble**（多个NN集成）
4. **尝试稀疏GP**（SGP）处理更大数据集

### 6.2 应用到MBPO

- 最佳方案(GP + 自适应残差缩放 = 0.081 rad)需要集成到MBPO的世界模型中
- 需要测试在MBPO训练循环中的实际效果
- 可能需要在线更新（新数据到来时重新训练）

---

## 七、相关代码

| 文件 | 说明 |
|------|------|
| `test_adaptive_scale.py` | 第一轮：自适应残差缩放 |
| `test_gp_nn_fusion.py` | 第二轮：GP+NN深度融合 |
| `test_multistep_loss.py` | 第三轮：多步损失训练 |
| `test_physics_constrained.py` | 第四轮：物理约束残差 |
| `test_bayesian_nn.py` | 第五轮：MC Dropout |

---

## 八、运行命令

```bash
# 运行所有改进测试
"E:/Anaconda/envs/DL/python.exe" "D:/系统辨识作业/sindy_bicycle/test_adaptive_scale.py"
"E:/Anaconda/envs/DL/python.exe" "D:/系统辨识作业/sindy_bicycle/test_gp_nn_fusion.py"
"E:/Anaconda/envs/DL/python.exe" "D:/系统辨识作业/sindy_bicycle/test_multistep_loss.py"
"E:/Anaconda/envs/DL/python.exe" "D:/系统辨识作业/sindy_bicycle/test_physics_constrained.py"
"E:/Anaconda/envs/DL/python.exe" "D:/系统辨识作业/sindy_bicycle/test_bayesian_nn.py"
```

---

*报告生成时间：2026-06-22*
*GitHub: https://github.com/Ma-mingliang/sindy-bicycle-system-id*
